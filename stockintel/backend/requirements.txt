# ---------------------------------------------------------------------------
# StockIntel backend dependencies
#
# Deliberately NOT installed into the repo-root .venv: that environment belongs
# to the pre-existing TensorFlow/Streamlit project and is left untouched.
# Create an isolated env for this backend:
#
#     python -m venv stockintel/backend/.venv
#     stockintel/backend/.venv/Scripts/pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- Web framework ---------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- Numerics / data -------------------------------------------------------
numpy>=1.26.0
pandas>=2.2.0
pyarrow>=17.0.0          # Parquet cache format

# --- Market data -----------------------------------------------------------
yfinance>=0.2.50
requests>=2.32.0

# --- Classical ML ----------------------------------------------------------
scikit-learn>=1.5.0
lightgbm>=4.5.0

# --- Deep learning + NLP ---------------------------------------------------
# torch drives BOTH the LSTM and FinBERT, so only one DL framework is needed.
# CPU-only wheels are sufficient; models here are small.
torch>=2.6.0
transformers>=4.45.0

# --- Utilities -------------------------------------------------------------
python-dateutil>=2.9.0
pytz>=2024.2
httpx>=0.27.0

# --- Testing ---------------------------------------------------------------
pytest>=8.3.0
