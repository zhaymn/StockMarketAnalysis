# Secrets — never commit
backend/.env

# Isolated backend virtualenv
backend/.venv/

# Caches (regenerated on demand)
backend/.cache/
backend/.pytest_cache/
__pycache__/
*.pyc

# Trained model artifacts (regenerated by the training pipeline)
backend/.artifacts/

# Frontend
frontend/node_modules/
frontend/.next/
frontend/out/
